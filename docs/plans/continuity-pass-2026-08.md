# Continuity pass (2026-08) — nc-tower

## Changes
- Redrew `img/app.svg` as single-ink `currentColor` radio-tower silhouette.
- Licence → `AGPL-3.0-or-later`.
- Navigation: added `<order>83</order>`; dropped nonstandard `<id>` / `<type>link</type>`.

## Verify
- Nav icon monochrome; unique nav order.
